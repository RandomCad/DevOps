# TODOs for fuchs

## Core-programm

### main.py

* [ ] evalute async functions and implement them
* [ ] implement a transaction like concept, so that e.g. if saving the
markdown-content of a note fails, but the converted html is already stored on
the hamster, no inconsistencies arise (because it get's rolled back)

## Tests

* [ ] revise unittests
  * [ ] extract tests on failure in seperate test cases (especially in test_main to test on all failing func calls)
* [ ] revise integration tests
  * [ ] use the return types instead of static typed values to check return values
  * [ ] widen test cases (e.g. for update_media with valid but unrelated note_id & media_id [invalid note])
